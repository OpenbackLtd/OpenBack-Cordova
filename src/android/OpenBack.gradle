repositories {
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

android {
    lintOptions {
        abortOnError false
    }
}

dependencies {
    compile 'com.google.code.gson:gson:2.4'
    compile 'com.google.android.gms:play-services-location:9.6.0'
    compile 'com.google.android.gms:play-services-gcm:9.6.0'
    compile(name:'OpenBack', ext:'aar') { 
        transitive = true
    }
}